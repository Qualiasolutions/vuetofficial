const {
  getDateStringFromDateObject,
  getTimeStringFromDateObject,
  getDatesBetween,
  getDateStringsBetween
} = require('utils/datesAndTimes');

const tape = require('tape');

tape.test('TEST TEST', (t) => {
  t.assert(true);
});
