import { ColorName } from 'constants/Colors';

export const fieldColorMapping = {
  DaysOff: 'white',
  School: 'white',
  SchoolBreak: 'white',
  default: 'almostWhite'
} as { [key: string]: ColorName };
